# Secrets — NEVER commit these
.env

# Runtime state / artifacts
state.json
state.tmp
screenshots/
*.log
# Generated hourly on the server — committing it would make `git pull` conflict
# during a deploy.
monitorflow.json
monitorflow.tmp
monitorflow.lock
duty_openids.json
duty_openids.tmp
# Daily health report (health_report.py): last-sent slot + single-sender lock
.health_report_state.json
.health_report_state.json.tmp
.health_report_state.lock

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.Python

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
